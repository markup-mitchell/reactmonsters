import React from 'react';

function PartGrid() {
  return(
    <div class='partGrid'>
      <PartRow(props.heads) />
      <PartRow(props.heads) />
      <PartRow(props.heads) />
      <PartRow(props.heads) />
    </div>
  
  )
}